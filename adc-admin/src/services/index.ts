// LoginService has been removed as ADC Admin now uses SSO authentication through the portal
// Authentication is now handled by the @repo/auth package
// Users should access ADC Admin through the portal at http://localhost:5173

export default null;